name: Test

# The build workflow needs API credentials, which pull requests from forks do not
# get. These tests decode recorded responses instead, so they need no secrets.
on:
  pull_request:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.9', '3.11', '3.13']
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt pytest pytest-asyncio mypy
      - name: Test
        run: python -m pytest tests -q
      - name: Type check
        run: mypy -p weheat.abstractions --install-types --non-interactive
